<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_trains_by_no"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="in.railish.railish.activities.TrainsByNoActivity">

    <EditText
        android:id="@+id/train_number_edit_text_1"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Train number / name"
        android:background="#ffffff" />

    <Button
        android:id="@+id/search_train_by_number_button"
        android:layout_marginTop="10dp"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="#1ABCEF"
        android:text="Search"
        android:textColor="#fff"
        android:textAppearance="?android:textAppearanceLarge"
        android:textStyle="bold"/>

</LinearLayout>
