<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_train"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="in.railish.railish.activities.TrainActivity"
    tools:showIn="@layout/activity_train"
    android:background="@color/cardview_light_background">

    <ListView
        android:id="@+id/train_choices_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:dividerHeight="10dp"
        android:divider="@null">

    </ListView>

    <!--

    <Spinner
        android:id="@+id/choose_class_spinner_1"
        android:layout_width="match_parent"
        android:layout_height="50dp">
    </Spinner>

    <Spinner
        android:id="@+id/choose_quota_spinner_1"
        android:layout_width="match_parent"
        android:layout_marginTop="20dp"
        android:layout_height="50dp">
    </Spinner>

    <Button
        android:id="@+id/get_predictions_for_booking_date_button"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="10dp"
        android:text="Get Predictions for Booking Date"
        android:textAppearance="?android:textAppearanceLarge"
        android:textColor="#ffffff"
        android:background="#1ABCEF"
        android:textStyle="bold"/>

        -->

</RelativeLayout>
